export interface Hero {
    name: string;
    id: number;
    age: number;
    gender?: string;
    favouriteColor?: string;
    superPower: string;
    food?: string;
}

// I can create type using an interface or a class.
